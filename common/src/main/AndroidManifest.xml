<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.artem.common">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/WinterSportCoachesTheme">
        <activity android:name="com.wintersportcoaches.common.registration.LoginActivity" />

        <service
            android:name="com.wintersportcoaches.common.service.SocketListenerService"
            android:enabled="true"
            android:exported="true"/>
        <receiver android:name="com.wintersportcoaches.common.service.MessagesBroadcastReciever">

            <intent-filter>
                <action android:name="@string/message_intent">
                </action>
            </intent-filter>

        </receiver>
    </application>

</manifest>